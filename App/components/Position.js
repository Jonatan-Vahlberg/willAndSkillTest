import React from 'react';

class Position extends React.Component {
  render() {
    const {
      acquisition_price,
      allocation,
      change_percent,
      created_at,
      instrument,
      kind,
    } = this.props;
    return null;
  }
}

export default Position;
